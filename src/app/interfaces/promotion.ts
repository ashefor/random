export interface Promotion {
  title: string;
  description: string;
  trigger?: string;
  trigger_value?: number;
  status: string;
  manufacturer_id: string;
  manufacturer_staff_id: string;
  product_id: string;
}
